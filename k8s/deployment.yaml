apiVersion: apps/v1
kind: Deployment
metadata:
  name: gh-test
spec:
  selector:
    matchLabels:
      app: gh-test
  template:
    metadata:
      labels:
        app: gh-test
    spec:
      containers:

      - name: gh-test
        image: devopsaes/ghatestakif:31


